import { theme } from 'antd';

// 默认主题样式
const themeDefault = {
  algorithm: theme.defaultAlgorithm,
  cssVar: true,
  token: {
    borderRadius: 2,
  },
  components: {
    Table: {},
    Input: {},
  },
};

export default themeDefault;
