- 文档统一采用 Markdown 标题层级组织，根目录以数字前缀（1.、2.）对入门类文档进行排序命名。
- 组件用法文档中每个控件均以「字段定义 + 代码示例」的形式展示，统一通过 `ctrl: { type: Ctrl.Xxx }` 或 `type: VType.LayoutXxx` 的配置对象声明 UI。
- 页面开发遵循 View/Data/Handler 三段式约定：入口使用 `<ViewRoot ViewClass={...} DataClass={...} HandlerClass={...} />`，数据与视图分别继承自 `DataBase` 与 `ViewBase`，事件处理继承 `HandlerBase`。